# 01.01.02: shadcn/ui init with one primitive

**Status:** done
**Parent:** ✅01.01-nextjs-scaffold-tailwind-shadcn.md
**Blocked by:** 01.01.01
**Unblocks:** 01.02 (once 01.01 is complete)

## Purpose

Establish the shadcn/ui component baseline so later tickets can add primitives.

## Scope

shadcn init (`components.json`, `lib/utils.ts`, theme tokens in global CSS,
`components/ui/*` baseline), one primitive (e.g. Button) rendered on the placeholder page.

## Out of scope

Any other primitives, shell components, env, DB, auth.

## Acceptance criteria

- [x] shadcn is initialised and one primitive renders correctly styled on `/`.
- [x] `dev`, `build` and `lint` still pass.

## Demo / verification

Run `dev`, open `/`, see the styled primitive; run `build` and `lint`.

## Review budget / risk

Small, mostly generated. Risk: theme token changes to global CSS touch a shared file.

## Comments

- 2026-09-24: Branch `feat/01.01.02-shadcn-init`. Implemented by hand (no generator):
  `components.json`, `lib/utils.ts`, `components/ui/button.tsx`, shadcn tokens in
  `app/globals.css` (dark mode kept on `prefers-color-scheme`), Button on the placeholder page.
  lint, tsc, build and audit clean; verified in browser (light screenshot, dark computed styles).
  Review: no blockers. Low notes accepted: Button has no default `type="button"` (upstream
  behavior), `lucide-react` is installed but unused until the first icon.
