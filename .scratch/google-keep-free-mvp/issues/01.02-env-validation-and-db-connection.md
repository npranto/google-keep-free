# 01.02: Validated env and Neon/Drizzle connection

**Status:** decomposed - implementation delegated to sub-tickets 01.02.01-01.02.04
**Parent:** 01-application-foundation.md
**Blocked by:** 01.01
**Unblocks:** 01.03

## Purpose

Fail fast on bad config and expose a working DB client.

## Scope

`lib/env.ts` (Zod-validated, initially `DATABASE_URL`), `.env.example`,
`lib/db/index.ts` (Drizzle + Neon), empty `lib/db/schema.ts` placeholder, drizzle config.

## Out of scope

Any tables, Clerk env vars.

## Acceptance criteria

- [ ] Missing or invalid `DATABASE_URL` fails startup with a clear error.
- [ ] A trivial `select 1` through the DB client succeeds against Neon.
- [ ] `.env.example` documents every variable in `lib/env.ts`.

## Demo / verification

Unset `DATABASE_URL` and observe the error; set it and run a `select 1`.

## Review budget / risk

Small. `lib/env.ts` is extended by 01.03, so keep its shape additive.

## Sub-tickets (umbrella)

This ticket is now an index. Implement the children in this order:

1. [ ] 01.02.01-neon-account-and-database-url.md (human step, no blockers)
2. [ ] 01.02.02-zod-validated-env.md (parallel with 01.02.01)
3. [ ] 01.02.03-drizzle-neon-client-select-1.md (blocked by 01.02.01, 01.02.02)
4. [ ] 01.02.04-drizzle-kit-config.md (blocked by 01.02.03)

## Completion checklist

- [ ] All four children complete
- [ ] All original acceptance criteria above verified

## Coverage map

- Missing or invalid `DATABASE_URL` fails startup: 01.02.02
- `select 1` succeeds against Neon: 01.02.01 (credential), 01.02.03 (client)
- `.env.example` documents every variable: 01.02.02
- `lib/db/index.ts`, empty `schema.ts`: 01.02.03
- Drizzle config: 01.02.04
